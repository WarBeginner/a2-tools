Содержимое

1. Win-32 консольное приложение для облегчения редактирования ресурсных файлов аллодов2 (путем перевода в более удобный для этого формат).
26.10.2019  20:20           291 572 data_xml2xls.exe

1а) DATA.XML (содержимое data.bin) --> Excel (XML 2003 format)
Образец запуска:	"data_xml2xls.exe data.xml a2data.xml"
Оба файла должны быть и правильного формата.

1б) Excel (XML 2003 format) --> DATA.XML (будущее содержимое data.bin)
Образец запуска:	"data_xml2xls.exe a2data.xml data2.xml /IMPORT"
Файл-приемник создается.


2. Готовый файл Excel (XML 2003 format). Страницы заполнены и отформатированы в первом приближении.
26.10.2019  20:08         2 492 520 a2data.xml

Все ячейки с ключевым содержимым защищены от изменения.
Что требует внимания:
Лист "Структура" задает порядок и содержание выгрузки. Защищен.
Первая строка на каждом листе данных содержит "внутренние" имена полей. Защищены.
Вторая строка на каждом листе данных содержит "человеческие" имена полей. Можно менять.
Названия листов "человеческие", но при изменении обязательно надо изменить нужное место листа "Структура", иначе таблица не выгрузится. НЕ защищены.
Имеются вложенные таблицы.
  На основном листе в ячейке ставится "ссылка" (По умолчанию - Номер строки в квадратных скобках).
  На листе вложенной таблицы первая колонка содержит такую же ссылку.
  Все строки со "вложенного" листа, содержащие то же значение ссылки (и колонки кроме ссылки),
    образуют вложенную таблицу в качестве содержимого соответствующей ячейки основного листа.
  Содержимое "ссылок" может быть любым текстом.

3. Исходники на Delphi 5 (но зависимостей очень мало).
 Выкладка 1.0, рабочая (скорее всего), но непричесанная.
26.10.2019  17:40             9 874 data_structure.pas
02.11.2019  23:21            19 469 data_xml2xls.dpr
26.10.2019  15:05            20 369 xls_work.pas
26.10.2019  13:23            21 233 xml_work.pas
